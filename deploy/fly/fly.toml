# Raw-TCP SSH accelerator in front of the OVH origin. Fly Anycast accepts the
# player at the nearest edge and backhauls to this Machine (put it in Toronto,
# next to OVH Canada). It terminates NOTHING — SSH stays end-to-end; this only
# forwards bytes. Set the app name with `fly launch` / `fly apps create`.
app = "sshfighter-relay"
primary_region = "yyz"     # Toronto — one short hop to OVH Canada

[build]
  dockerfile = "Dockerfile"

[[services]]
  internal_port = 2222
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    port = 22
    handlers = []          # raw TCP — do NOT terminate TLS/SSH

  [services.concurrency]
    type = "connections"
    soft_limit = 800
    hard_limit = 1000

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"

# Web: forward the domain's HTTP/HTTPS to the origin's Caddy (raw TCP passthrough,
# TLS stays end-to-end). This is what makes https://sshfighter.com reach the site.
[[services]]
  internal_port = 8080
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    port = 80
    handlers = []

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"

[[services]]
  internal_port = 8443
  protocol = "tcp"
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [[services.ports]]
    port = 443
    handlers = []

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"
